@model PagedList.IPagedList<milkTea.Models.User_Accounts>

@using PagedList.Mvc;

@{
    ViewBag.Title = "ManageAccount";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section jsFooter{

    <script src="/Assets/js/AjaxCreateAccount.js"></script>
}
<div class="p-2">
    <div class="row">
        <div class="col-lg-12">
            <div class="p-3 panel panel-default">
                <div class="panel-heading">
                    Danh sách người dùng
                </div>
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover" style="border:2px; background-color:white;text-align:center;">
                            <thead>
                                <tr style="color:black">
                                    <th>Tài khoản</th>
                                    <th>Mật khẩu</th>
                                    <th>Loại tài khoản</th>
                                    <th>Họ</th>
                                    <th>Tên</th>
                                    <th>Email</th>
                                    <th>Số điện thoại</th>
                                    <th>Địa chỉ</th>
                                    <th>Avartar-url</th>
                                    <th>#</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="@item.Username">
                                        <td>@item.Username</td>
                                        <td>******</td>
                                        @{
                                            if (item.Type == 1)
                                            {
                                                <td>Admin</td>
                                            }
                                            else if (item.Type == 2)
                                            {
                                                <td>Người bán</td>
                                            }
                                            else
                                            {
                                                <td>Người mua</td>
                                            }
                                        }
                                        <td>@item.FirstName</td>
                                        <td>@item.LastName</td>
                                        <td>@item.Email</td>
                                        <td>@item.PhoneNumber</td>
                                        <td>@item.Address</td>
                                        <td>@item.Avatar_url</td>
                                        <td>
                                            <a href="#">Xóa</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>

                        @Html.PagedListPager(Model, Page => Url.Action("ManageAccount", new { Page }))

                    </div>
                </div>
                <a class="nav-item no-arrow p-2" href="/admin/createaccount" style="background-color:black;text-decoration:none;color:white;border-radius:15px">
                    Tạo tài khoản mới
                </a>
            </div>
        </div>
    </div>
</div>
